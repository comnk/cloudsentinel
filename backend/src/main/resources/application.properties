spring.application.name=astraquant-backend
spring.datasource.url=jdbc:postgresql://localhost:5432/astraquant-db
spring.datasource.username=${DB_USERNAME:astraquant}
spring.datasource.password=${DB_PASSWORD:astraquant}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.format_sql=true
spring.jpa.show-sql=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

spring.config.import=optional:file:.env[.properties]

spring.ai.google.genai.api-key=${GEMINI_KEY}
spring.ai.google.genai.chat.options.model=gemini-3-flash-preview

jwt.secret=${JWT_SECRET}
jwt.expiration=3600000

agent.service.url=${AGENT_SERVICE_URL:http://localhost:8000}

spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=group_id
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer